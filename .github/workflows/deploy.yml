name: Deploy PSMO to CyberPanel

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: psmo-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deployment secrets
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USERNAME: ${{ secrets.DEPLOY_USERNAME }}
          DEPLOY_PASSWORD: ${{ secrets.DEPLOY_PASSWORD }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        run: |
          missing=""
          [ -n "$DEPLOY_HOST" ] || missing="$missing DEPLOY_HOST"
          [ -n "$DEPLOY_USERNAME" ] || missing="$missing DEPLOY_USERNAME"
          [ -n "$DEPLOY_PASSWORD" ] || missing="$missing DEPLOY_PASSWORD"
          [ -n "$DEPLOY_PATH" ] || missing="$missing DEPLOY_PATH"
          [ -n "$DATABASE_URL" ] || missing="$missing DATABASE_URL"
          [ -n "$SESSION_SECRET" ] || missing="$missing SESSION_SECRET"
          if [ -n "$missing" ]; then
            echo "Missing GitHub Actions secrets:$missing"
            echo "Add them under Settings > Secrets and variables > Actions."
            exit 1
          fi

      - name: Upload application files
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          password: ${{ secrets.DEPLOY_PASSWORD }}
          source: "package.json,package-lock.json,next.config.ts,postcss.config.mjs,tsconfig.json,drizzle.config.ts,drizzle,public,src"
          target: ${{ secrets.DEPLOY_PATH }}
          overwrite: true
          rm: false

      - name: Install, migrate, build, and restart
        uses: appleboy/ssh-action@v1.2.2
        env:
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          PM2_NAME: ${{ secrets.DEPLOY_PM2_NAME }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          password: ${{ secrets.DEPLOY_PASSWORD }}
          envs: DEPLOY_PATH,PM2_NAME,DATABASE_URL,SESSION_SECRET
          script: |
            set -e
            cd "$DEPLOY_PATH"
            export DATABASE_URL SESSION_SECRET NODE_ENV=production
            printf 'DATABASE_URL=%s\nSESSION_SECRET=%s\nNODE_ENV=production\n' "$DATABASE_URL" "$SESSION_SECRET" > .env.production
            chmod 600 .env.production
            npm ci --include=dev
            npx drizzle-kit push
            npm run build
            if [ -n "$PM2_NAME" ]; then
              pm2 restart "$PM2_NAME" --update-env
            elif pm2 describe psmo >/dev/null 2>&1; then
              pm2 restart psmo --update-env
            else
              pm2 start npm --name psmo -- start
            fi
            pm2 save
